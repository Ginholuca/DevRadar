import React from 'react';

import ReactDOM from 'react-dom'; //ReactDOM deixa o React se comunicar com a DOM
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //rendereizando a conteudo do app na id root

//JSX (JS+XML)
//XML == Sintaxe do HTML
